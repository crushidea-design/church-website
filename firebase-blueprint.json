{
  "entities": {
    "User": {
      "title": "User",
      "description": "A registered user of the church website.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["admin", "user"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "displayName", "role", "createdAt"]
    },
    "Post": {
      "title": "Post",
      "description": "A post in the Research Lab or Community.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "content": { "type": "string" },
        "category": { "type": "string", "enum": ["research", "community", "sermon", "journal"] },
        "subCategory": { "type": "string", "enum": ["worship", "preaching", "pastoring", "governing", "general", "past_sermons", "pilgrims_progress"] },
        "sermonCategoryId": { "type": "string" },
        "researchCategoryId": { "type": "string" },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "commentCount": { "type": "number" },
        "pdfUrl": { "type": "string" },
        "pdfName": { "type": "string" },
        "pdfBase64": { "type": "string" },
        "journalDate": { "type": "string", "format": "date-time" }
      },
      "required": ["title", "content", "category", "authorId", "authorName", "createdAt", "updatedAt", "commentCount"]
    },
    "SermonCategory": {
      "title": "SermonCategory",
      "description": "A category for sermon videos (playlists).",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "order": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "order", "createdAt"]
    },
    "ResearchCategory": {
      "title": "ResearchCategory",
      "description": "A category for research lab posts.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "order": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "order", "createdAt"]
    },
    "Comment": {
      "title": "Comment",
      "description": "A comment on a post.",
      "type": "object",
      "properties": {
        "postId": { "type": "string" },
        "content": { "type": "string" },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["postId", "content", "authorId", "authorName", "createdAt"]
    },
    "Contact": {
      "title": "Contact",
      "description": "A contact message from a visitor.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "message": { "type": "string" },
        "read": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "message", "read", "createdAt"]
    },
    "PrayerRequest": {
      "title": "PrayerRequest",
      "description": "A prayer request in the prayer room.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "authorName": { "type": "string" },
        "content": { "type": "string" },
        "isPrivate": { "type": "boolean" },
        "prayCount": { "type": "number" },
        "prayedBy": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "authorName", "content", "isPrivate", "prayCount", "prayedBy", "createdAt"]
    },
    "Setting": {
      "title": "Setting",
      "description": "Global application settings and church information.",
      "type": "object",
      "properties": {
        "heroImageUrl": { "type": "string" },
        "introImageUrl": { "type": "string" },
        "introTitle1": { "type": "string" },
        "introContent1": { "type": "string" },
        "introTitle2": { "type": "string" },
        "introContent2": { "type": "string" },
        "quote": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["updatedAt"]
    },
    "ActivityLog": {
      "title": "ActivityLog",
      "description": "A log of user activity on the site.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string" },
        "activityType": { "type": "string" },
        "pagePath": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role", "activityType", "pagePath", "timestamp"]
    },
    "FCMToken": {
      "title": "FCMToken",
      "description": "A Firebase Cloud Messaging token for a user device.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "token": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "token", "updatedAt"]
    }
  },
  "firestore": {
    "fcm_tokens/{tokenId}": {
      "schema": { "$ref": "#/entities/FCMToken" },
      "description": "Stores FCM tokens for push notifications."
    },
    "activity_logs/{logId}": {
      "schema": { "$ref": "#/entities/ActivityLog" },
      "description": "Stores user activity logs."
    },
    "users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles and roles."
    },
    "posts/{postId}": {
      "schema": { "$ref": "#/entities/Post" },
      "description": "Stores research articles and community posts."
    },
    "comments/{commentId}": {
      "schema": { "$ref": "#/entities/Comment" },
      "description": "Stores comments on posts."
    },
    "contacts/{contactId}": {
      "schema": { "$ref": "#/entities/Contact" },
      "description": "Stores contact messages."
    },
    "prayer_requests/{requestId}": {
      "schema": { "$ref": "#/entities/PrayerRequest" },
      "description": "Stores prayer requests in the prayer room."
    },
    "settings/{settingId}": {
      "schema": { "$ref": "#/entities/Setting" },
      "description": "Stores global application settings."
    },
    "sermon_categories/{categoryId}": {
      "schema": { "$ref": "#/entities/SermonCategory" },
      "description": "Stores categories for sermon videos."
    },
    "research_categories/{categoryId}": {
      "schema": { "$ref": "#/entities/ResearchCategory" },
      "description": "Stores categories for research lab posts."
    }
  }
}
